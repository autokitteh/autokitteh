# DANGER DANGER DANGER

DO NOT USE. NOT READY FOR GA.


## Instructions

1. Make sure Temporal is running first
2. Start autokitteh: `./scripts/akd-docker.sh --setup`
3. Start web dashboard: `cd web/dashboard` and `npm start`
4. `http://localhost:3000/dash`

this is in flux - the newest ui is being developed in `/ui`.