foo: meow

-- main.flowchart.yaml start --
version: v1

imports:
  - path: "foo.star"
    name: foo

nodes:
  - name: start
    action:
      call:
        target: imports.foo.say
        args:
          msg: "meow"

-- foo.star --
def say(msg):
  print("foo: " + msg)

