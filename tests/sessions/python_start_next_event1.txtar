COMPLETED meow

-- main.py:main --
from autokitteh import start, next_event

def main(event):
    sid = start('main.py:child')
    state = next_event(sid)['state']
    print(state['type'], state['value'])

def child(event):
    return "meow"
