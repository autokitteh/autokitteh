42

-- main.flowchart.yaml main --
version: v1

options:
  interpolate_all_strings: true

imports:
  - name: foo
    path: "foo.star"

nodes:
  - name: main
    result: 42
    goto: nodes.loop

  - name: loop
    action:
      foreach:
        items: [1]
        call:
          target: nodes.iter

  - name: iter
    action:
      call:
        target: imports.foo.bar
        args:
          answer: states.results.main

-- foo.star --
def bar(answer):
  print(answer)
