bar
end

-- main.flowchart.yaml main --
version: v1

options:
  interpolate_all_strings: true

imports:
  - name: foo
    path: "foo.star"

nodes:
  - name: main
    action:
      call:
        target: nodes.iter
    goto: nodes.end

  - name: iter
    action:
      call:
        target: imports.foo.bar

  - name: end
    action:
      print:
        value: "'end'"

-- foo.star --
def bar():
  print("bar")
