meow, world!

-- main.flowchart.yaml main --
version: v1

options:
  interpolate_all_strings: true

imports:
  - name: foo
    path: "foo.star"

nodes:
    - name: main
      action:
        call:
          target: imports.foo.say
          args:
            sound: "'meow'"
      goto: nodes.end

    - name: end
      action:
        print:
          value: "states.nodes.main"

-- foo.star --
def say(sound):
  return "{}, world!".format(sound)
