ERROR error: division by zero

-- main.py:main --
from autokitteh import start, next_event

def main(event):
    sid = start('main.py:child')
    state = next_event(sid)['state']
    print(state['type'], state['value'].split("\n")[0])

def child(event):
    return 1/0
