


# ak trigger create (all except name) -> error

# ak trigger create --name my_trigger (all except proj and env) -> error

# ak trigger create --name my_trigger --project my_project_name/id ... -> success

# ak trigger create --name my_trigger --env my_env_name/id ... -> success

# ak trigger create --name my_trigger --project my_project_name/id --env my_env_name/id ... -> success


#env:
#If the input is empty, we return nil but not an error
#If the environment is empty but the project isn't, we try to resolve the environment as the default one for the project
#If the environment is specified as a name, the project is required
#If the environment is specified as a *full* name, the project is optional, but if specified it must concur with the project prefix in the environment name
#If the environment is specified as an ID, the project is optional, but if specified it must concur with the environment's known project



#❯ ./bin/ak trigger create --name my_trigger
#Error: required flag(s) "call" not set

#❯ ./bin/ak trigger create --name my_trigger --call foo.star:func
#Error: at least one of the flags in the group [project env] is required

#❯ ./bin/ak trigger create --name my_trigger --call foo.star:func --project gmail_sample
#Error: required flag(s) "connection", "event" not set
