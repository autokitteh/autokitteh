Determine if the provided attachment content is an invoice.
If an invoice, extract invoice id, list of items with their amounts, VAT (if any) and compute the total.

Return a JSON with the following structure:
{
    isInvoice: boolean,
    invoiceId: string,
    items: [{description: string, amount: number}],
    vat: number,
    total: number
  }


Ensure the response is valid JSON without any additional text or formatting.
