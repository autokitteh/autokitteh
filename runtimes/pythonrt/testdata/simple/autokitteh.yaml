version: v1

project:
  name: py_simple
  triggers:
    - name: events
      event_type: post
      call: simple.py:greet
      webhook: {}
  vars:
    - name: USER
      value: Garfield

# Invoke the trigger with:
#   curl -d HELLO http://localhost:9980/http/py_simple/events
